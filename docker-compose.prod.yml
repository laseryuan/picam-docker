version: '2'

services:
  busybox:
    image: hypriot/armhf-busybox
    command: tail -f /dev/null
    # volumes_from:
      # - picam
      # - rtmp
  rtmp:
    image: lasery/watchdog:rtmp
    devices:
      - "/dev/vchiq:/dev/vchiq"

  picam:

  frp-server:

  frp-client:

  dev-cli:
    image: tianon/true
    restart: "no"
    depends_on:
      - picam
      - rtmp
